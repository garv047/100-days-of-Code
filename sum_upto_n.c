#include <stdio.h>

int main(int argc, char** argv[]) {
    int n;
    int sum;

if(n >= 1);

    printf("Enter numbers:\n");
    scanf("%d", &n);

    sum = (n * (n + 1)) / 2;

    printf("sum of natural numbers upto %d is: %d \n",n, sum);

    return 0;
};
